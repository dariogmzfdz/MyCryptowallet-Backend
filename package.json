{
  "name": "my-cryptowallet-backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node-dev src/index.ts",
    "lint": "ts-standard src/**/*.ts",
    "start": "node build/index.js",
    "deploy": "nodemon -L -e ts --exec \"npm run tsc && npm start\"",
    "tsc": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "cors": "^2.8.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.2",
    "uuid": "^9.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^2.0.21"
  }
}
